<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Search <%= q %></title>
  <meta name="description" content="Results for <%= q %>" />
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="icon" href="/img/favicon.ico" type="image/x-icon" />
<link rel="stylesheet" href="/css/global.css">
</head>
<body>
  <div class="mainContainer">
    <%- include("partials/header") %>

    <main class="content">
      <h1>Results for “<%= q %>”</h1>

      <% if (!q) { %>
        <p>Search for something...</p>
      <% } else if (results.length) { %>
        <p><strong><%= results.length %></strong> result<%= results.length === 1 ? "" : "s" %></p>
        <div class="list">
          <% results.forEach(p => { %>
            <div class="result-item">
              <hr>
              <a href="/<%= p.page_id %>" class="searchTitle"><%= p.title %></a>
              <p class="searchDesc"><%= p.description %></p>
            </div>
          <% }) %>
        </div>

      <% } else { %>
           <hr>
        <p>No pages matched your search.</p>
      <% } %>
    </main>
    <%- include("partials/footer") %>
  </div>
</body>
</html>
